export interface WorkflowsIds {
  id: string;
}

export interface Token {
  WEBAPP: string;
  URL_WEABPP: string;
  CLIENT_ID_WEBAPP: string;
}

export interface LogColors {
  Red: string;
  Green: string;
  Yellow: string;
  Blue: string;
  Cyan: string;
  Magenta: string;
  White: string;
  Reset: string;
}

export interface StringReplacement {
  from: string;
  to: string;
}